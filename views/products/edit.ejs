<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
</head>
<body>
  <h3>Edit Product</h3>
  <form action="/products/<%= product._id %>?_method=PUT" method="POST">
    <label for="name">Product Name</label>
    <input id="name" type="text" name="name" value="<%= product.name %>" placeholder="Product Name" />
    <label for="price">Price</label>
    <input id="price" type="number" name="price" value="<%= product.price %>" step="0.01" placeholder="Product Price" />
    <select name="category">
      <% for(const category of categories) { %>
            <option value="<%= category %>" <%= category === product.category ? "selected" : "" %>><%= category %></option>
      <% } %>
    </select>
    <button type="submit">Edit Product</button>
  </form>
</body>
</html>